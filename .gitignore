# Environment / secrets
.env
app/.env
*.env.local

# NocoBase runtime storage & Postgres data (mounted volumes)
app/storage/

# Node
node_modules/
dist/
.pnpm-store/

# company-management plugin has no CI build step (production container lacks
# a TypeScript toolchain), so its compiled output is committed deliberately.
!plugins/company-management/dist/
!plugins/company-management/dist/**

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
